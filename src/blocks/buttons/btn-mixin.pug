mixin purple-with-arrow(name,href,id)
    .filled-block
        button(class="filled-block__btn" id=id href=href)=name
            .filled-block__arrow
                <i class="fas fa-arrow-right"></i>

mixin  white-gradient(name,href,id)
    .login-btn__border
        a(class="login-btn__login" id=id href=href)=name

mixin purple-btn(name,href,id)
    .filled-btn
        button(class="filled-block__btn" id=id href=href)=name

mixin register-btn(name,href,id)
    button(href=href id=id class="register-but__btn")=name


mixin purple-with-arrow(name)
        .find-number
            a(href="#")= name
                .arrow
                    img(src="/src/img/svg/next-arrow.svg")

mixin  white-gradient(name)
    .login-btn
        a(class="login-btn__login" hred="#")=name